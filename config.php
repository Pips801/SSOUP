<?php
// @TODO: Config page. Probably gunna need to use SQL allow to store and modify these vairiables outside the script.


$file_size_limit = "50"; // the maximum file size in MB.

$allowed_directories = ["test", "img"]; // the directories that can have a file uploaded to.

$administrator_PIN = "1234"; // the PIN that authenticate the upload.

?>  